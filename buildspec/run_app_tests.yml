version: 0.2

phases:
  install:
    runtime-versions:
      ruby: 3.4
      nodejs: 20
    commands:
      - bundle install
      - npm ci
      - make hosts
  build:
    commands:
      - make frontend-build
      - make test
