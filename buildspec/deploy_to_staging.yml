version: 0.2

runtime-versions:
  ruby: 2.6
  nodejs: 10

env:
  variables:
    DEPLOY_APPNAME: "mhclg-epb-frontend-ui-staging"

  parameter-store:
    PAAS_LOGIN_STAGING: "epbr-govpaas-staging-login"
    PAAS_PW_STAGING: "epbr-govpaas-staging-pw"
    PAAS_SPACE_STAGING: "epbr-govpaas-staging-space"
    PAAS_API: "epbr-govpaas-api"
    PAAS_ORG: "epbr-govpaas-orgname"

phases:
  install:
    runtime-versions:
      ruby: 2.6
      nodejs: 10
    commands:
      - gem install bundler:2.0.2
      - bundle install
      - npm install
  build:
    commands:
      - make frontend-build
      - scripts/generate-paas-manifest.sh $DEPLOY_APPNAME > manifest.yml
      - echo ">>>>>>>> DEPLOY TO STAGING ENV"
      - cf login -a $PAAS_API -u $PAAS_LOGIN_STAGING -p "$PAAS_PW_STAGING" -o $PAAS_ORG -s $PAAS_SPACE_STAGING
      - cf v3-apply-manifest -f manifest.yml
      - cf v3-zdt-push $DEPLOY_APPNAME --wait-for-deploy-complete
      - cf logout
